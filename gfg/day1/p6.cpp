#include <bits/stdc++.h>
using namespace std;
int main()
{
    set<int>s(4);
    int n, k;
    cin >> n >> k;
    int arr[n];

    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    // sort(arr, arr+n);
    // cout << arr[n - k - 1];
    for(int i=0;i<n;i++){
       if(set.size()<k){
        
       }
    }
}
